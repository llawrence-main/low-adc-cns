function [encl_list,suffix_list] = get_encl_list()
% returns the list of enclosing ROIs for the low-ADC region

encl_list = {'GTVDaily'}; % list of enclosing ROI names

suffix_list = {''}; % suffixes for directories containing low-ADC ROIs


end